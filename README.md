# Solana Memecoin Bot

## Quick Setup

```bash
chmod +x setup.sh
./setup.sh
```

Then edit `config/local.toml` with your API keys and run:

```bash
cargo run --release --bin bot
```

Dashboard: http://localhost:8080

See full documentation in the repository.
